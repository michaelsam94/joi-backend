node_modules
dist
.env
*.log
coverage
/uploads
# The Google service-account key, bind-mounted into the backend container by docker-compose.yml —
# lives only on the VPS host, never in the repo.
/google-service-account.json
